/** Keeps `@lumina/shared-types` wired into json-view for future catalog typing. */
export type { LuminaRole, ProductKind } from "@lumina/shared-types";

export const LUMINA_JSON_VIEW = "json-view" as const;
